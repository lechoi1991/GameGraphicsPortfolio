# 눈 내리는 도시의 테이저 액션 이펙트 구현 설명서

## 프로젝트 개요

본 프로젝트는 **눈 내리는 야간 도시에서 경찰이 테이저를 발사하고, 전기 에너지가 도둑에게 이동한 뒤 피격 순간 강한 전기 효과가 발생하는 장면**을 구현하는 것을 목표로 한다.

이를 위해 Unity의 **Particle System, Shader Graph, Visual Effect Graph, PBR Material, C# 스크립트**를 각각 목적에 맞게 사용하였다.

## 테이저 발사체 — Particle System

이 효과는 경찰이 테이저를 발사했을 때 생성되는 공격 이펙트이다.

### 사용 목적

테이저 공격을 단순한 선이나 즉시 발생하는 효과로 표현하지 않고, **경찰에서 도둑까지 실제로 이동하는 공격**으로 보여주기 위해 Particle System을 사용하였다.

이를 통해 사용자는 공격이 어느 방향으로 이동하고 있는지 쉽게 파악할 수 있다.

### 발사체의 역할

테이저 발사체는 다음과 같은 공격 흐름을 가진다.

```text
경찰
 ↓
TaserProjectile 생성
 ↓
도둑의 TaserHitPoint 방향으로 이동
 ↓
목표 지점 도달
 ↓
적중 이벤트 발생
```

Particle System 자체는 발사체의 시각적인 표현을 담당하고, 실제 이동과 적중 판단은 C# 스크립트가 담당한다.

이처럼 **시각 표현과 게임 로직을 분리**하였다.

## TaserProjectile 스크립트

`TaserProjectile.cs`는 생성된 테이저 발사체가 목표 지점으로 이동하도록 관리한다.

### 주요 역할

* 목표 Transform 저장
* 목표 방향 계산
* 목표를 향해 이동
* 목표와 충분히 가까워지면 적중 처리
* 적중 이벤트 호출
* 자신의 중복 적중 방지
* 적중 후 발사체 제거

발사체가 목표를 직접 참조하도록 구성한 이유는 **미리 정해진 위치로 이동하는 것이 아니라 실제 목표 위치를 기준으로 공격하도록 하기 위해서**이다.

### 중복 적중 방지

발사체에는 한 번 적중한 이후 다시 적중 처리가 실행되지 않도록 상태를 관리하였다.

이는 하나의 공격으로 여러 번 피격 이벤트가 발생하는 문제를 방지한다.

따라서 하나의 발사체는 다음과 같이 동작한다.

```text
생성
 ↓
이동
 ↓
적중
 ↓
적중 이벤트 1회
 ↓
제거
```

이 구조는 과제의 **하나의 이벤트에서 불필요하게 동일한 이펙트를 반복 생성하지 않는다**는 요구사항에도 대응한다.

---

## TaserController

`TaserController.cs`는 테이저 공격 전체를 관리하는 컨트롤러이다.

현재는 입력 시스템을 이용하여 Space 입력을 공격 이벤트로 사용하였다.

### 사용 목적

사용자의 입력과 이펙트를 연결하여 단순히 씬에 이펙트를 배치하는 것이 아니라 **실제 게임 이벤트에 의해 효과가 발생하도록** 하기 위해서이다.

공격 과정은 다음과 같다.

```text
Space 입력
 ↓
FireTaser()
 ↓
TaserProjectile 생성
 ↓
목표를 향해 이동
 ↓
적중
 ↓
OnTaserHit()
```

이 구조를 통해 과제에서 요구하는 **Input Event와 Effect의 연결**을 구현하였다.

## 공격 중복 방지

`TaserController`에서는 이미 발사 중인 경우 추가 공격을 생성하지 않도록 상태를 관리한다.

이 기능을 넣은 이유는 사용자가 공격 버튼을 빠르게 반복해서 입력했을 때 동일한 이벤트가 여러 번 실행되어 발사체가 무분별하게 생성되는 것을 막기 위해서이다.

따라서 하나의 공격은 하나의 발사체와 하나의 적중 이벤트를 중심으로 처리된다.

```text
입력
 ↓
공격 시작
 ↓
발사체 생성
 ↓
공격 진행 중
 ↓
적중
 ↓
다음 공격 가능
```

---

## 테이저 전기 빔 — Shader Graph

테이저가 경찰에서 도둑까지 연결되는 전기 에너지를 표현하기 위해 `ElectricShader` Shader Graph를 제작하였다.

Shader Graph를 사용한 이유는 단순한 단색 재질보다 **시간에 따라 움직이는 에너지와 불규칙한 전기 느낌을 표현하기 위해서**이다.

---

### Shader Graph의 주요 구성

Shader Graph에서는 다음 기능을 조합하였다.

```text
UV
 ↓
Tiling / Offset
 ↓
Noise
 ↓
색상과 결합
 ↓
투명도 조절
 ↓
최종 전기 재질
```

또한 시간 정보를 UV 흐름에 활용하여 정적인 재질이 아니라 **계속 움직이는 전기 에너지처럼 보이도록** 구성하였다.

### Noise를 사용한 이유

전기 효과는 완전히 균일한 선보다 불규칙하게 움직이는 형태가 더 자연스럽다.

따라서 Noise를 사용하여 빔의 표면에 불규칙한 밝기 변화를 만들었다.

이것은 전기 에너지가 일정한 형태로 흐르는 것이 아니라 **끊임없이 흔들리고 변화하는 느낌**을 표현하기 위한 것이다.

### UV Animation을 사용한 이유

전기 빔이 완전히 정적인 상태라면 일반적인 막대나 광선처럼 보일 수 있다.

시간에 따라 UV가 움직이도록 구성하여 Shader 자체가 지속적으로 변화하도록 만들었다.

이를 통해 별도의 복잡한 애니메이션 없이도 **전기가 흐르는 듯한 움직임**을 표현할 수 있다.

### Alpha를 사용한 이유

전기 빔의 가장자리를 완전히 불투명한 형태로 만들기보다 일부 영역을 투명하게 표현하기 위해 Alpha를 사용하였다.

이러한 투명도 변화는 전기 효과가 단단한 물체가 아니라 **빛과 에너지로 이루어진 효과처럼 보이게 하는 역할**을 한다.

## TaserBeam

Shader Graph로 제작한 `Electric_Mat`은 실제 `TaserBeam` 오브젝트에 적용하였다.

`TaserBeam`은 경찰과 도둑 사이의 두 지점을 연결하는 전기 빔이다.

### 사용 목적

테이저 발사체만 이동시키는 경우 공격의 경로가 순간적으로 잘 보이지 않을 수 있다.

따라서 경찰과 도둑 사이를 시각적으로 연결하는 빔을 추가하여:

* 공격 방향
* 공격 범위
* 공격 대상
* 공격 진행 상태

를 한눈에 파악할 수 있도록 하였다.

### TaserBeam의 위치 계산

`TaserBeamController.cs`는 경찰의 `TaserMuzzle`과 도둑의 `TaserHitPoint`를 기준으로 빔의 위치와 방향을 자동으로 계산한다.

두 지점의 관계는 다음과 같다.

```text
Start = TaserMuzzle
End   = TaserHitPoint
```

두 지점 사이의 중간 위치에 Beam을 배치하고, 두 지점을 연결하는 방향으로 회전시킨다.

또한 두 지점 사이의 거리에 따라 빔의 길이를 조절한다.

이렇게 구성한 이유는 캐릭터의 위치가 변경되더라도 **항상 실제 발사 위치와 피격 위치를 정확하게 연결하기 위해서**이다.

---

## 테이저 적중 시 Visual Effect Graph

도둑에게 테이저가 적중하면 `ThiefElectricVFX`가 생성된다.

이 효과는 Visual Effect Graph를 사용하였다.

### 사용 목적

테이저 피격 순간에는 단순한 하나의 파티클보다 여러 방향으로 튀어나가는 전기 입자와 순간적인 에너지 효과가 필요하다.

Visual Effect Graph는 이러한 복수의 입자와 움직임을 노드 기반으로 구성하기 적합하기 때문에 피격 이펙트에 사용하였다.

### 피격 VFX의 위치

VFX는 `TaserHitPoint`를 기준으로 생성하도록 구성하였다.

```text
Thief
└─ TaserHitPoint
```

를 피격 효과의 기준 위치로 사용하는 이유는 **테이저의 목표 지점과 전기 피격 효과가 정확히 같은 위치에서 발생하도록 하기 위해서**이다.

### VFX Graph Prefab

`ThiefElectricVFX`는 Prefab으로 만들어 관리하였다.

Prefab으로 만든 이유는 씬에 직접 효과를 고정해서 배치하기보다 **실제 적중 이벤트가 발생했을 때 필요한 위치에 생성하기 위해서**이다.

구조적으로는 다음과 같다.

```text
Taser Hit Event
      ↓
VFX Prefab 생성
      ↓
TaserHitPoint 위치에 배치
      ↓
VFX 실행
```

이렇게 구성하면 향후 동일한 피격 효과를 다른 캐릭터나 다른 공격 시스템에서도 재사용할 수 있다.

### 적중 이벤트와 VFX 연결

테이저 적중 시 `TaserController`의 `OnTaserHit()`가 실행된다.

이 함수에서:

```text
Taser 공격 성공
       ↓
Beam 종료
       ↓
Hit VFX 생성
       ↓
VFX 실행
```

순서로 처리하도록 구성하였다.

이를 통해 **공격 이벤트와 시각 효과를 직접 연결**하였다.

즉, VFX가 단순히 일정 시간마다 발생하는 것이 아니라 실제 공격 성공이라는 게임 이벤트를 기반으로 실행된다.

---

### VFX 중복 생성 방지

현재 활성화되어 있는 피격 VFX를 `activeHitVfx`로 관리한다.

새로운 적중이 발생했을 때 기존 VFX가 남아 있다면 이를 먼저 제거한 뒤 새로운 VFX를 생성한다.

이를 통해:

```text
공격 1회
 ↓
피격 VFX 1개
```

라는 관계를 유지할 수 있다.

이는 하나의 공격 이벤트가 발생할 때 같은 효과가 불필요하게 여러 개 생성되는 것을 방지하기 위한 것이다.

---

## PBR Material 구성

도시 환경의 재질 표현을 위해 PBR 기반 재질을 사용하였다.

대표적으로 도로와 차량에 서로 다른 재질 특성을 적용하였다.

### 도로 재질

도로는 야간의 젖은 도로를 표현하기 위해 비교적 매끄러운 표면 특성을 강조하였다.

이를 통해 가로등과 주변 빛이 도로에 반사되는 느낌을 줄 수 있다.

### PBR Material 비교

현재 씬에서는 PBR의 대표적인 속성을 서로 다른 목적에 맞게 사용하였다.

| 대상           | 주요 표현 방향      | 목적           |
| ------------ | ------------- | ------------ |
| Road         | 높은 표면 반사 특성   | 젖은 야간 도로 표현  |
| Street Light | 발광 특성         | 야간 조명 표현     |

이를 통해 PBR Material이 단순히 색상을 지정하는 용도가 아니라 **재질의 물리적인 특성을 표현하는 데 사용되었음**을 확인할 수 있다.

## 전체 이펙트 동작 구조

현재 구현된 전체 시스템은 다음과 같은 흐름으로 연결된다.

```text
                    [Space 입력]
                         │
                         ▼
                 TaserController
                         │
                         ▼
                TaserProjectile 생성
                         │
                         ▼
              TaserHitPoint 방향 이동
                         │
                         ├───────────────┐
                         │               │
                         ▼               ▼
                    TaserBeam        적중 판정
                   Shader Graph          │
                         │               ▼
                         │          OnTaserHit()
                         │               │
                         │        ┌──────┴──────┐
                         │        ▼             ▼
                         │    Beam 종료     VFX 생성
                         │                      │
                         │                      ▼
                         │              ThiefElectricVFX
                         │                      │
                         │                      ▼
                         │                  전기 피격
                         │
                         ▼
                    전기 공격 표현
```

이 구조의 장점은 각각의 시스템이 서로 다른 역할을 담당하면서도 최종적으로 하나의 공격 연출로 연결된다는 것이다.


## 과제 요구사항 대응

### Graphic Concept

눈 내리는 도시에서 경찰이 도둑에게 테이저를 발사하고 도둑이 전기 공격에 피격되는 상황을 구현하였다.

### Effect Spatial Placement

테이저는 경찰의 발사 위치에서 시작하여 도둑의 피격 위치로 이동하도록 구성하였다.

피격 이펙트 역시 목표 지점에서 발생하도록 하여 공격의 시작과 종료를 명확하게 표현하였다.

### Shader Graph

전기 빔에 Shader Graph 기반 재질을 적용하였다.

Noise를 통해 불규칙한 전기 패턴을 만들고 UV Animation을 통해 흐르는 움직임을 표현하였다. Alpha를 이용하여 에너지의 투명한 특성을 표현하였다.

### PBR Material

도로와 차량에 서로 다른 PBR 특성을 적용하여 젖은 도로와 금속 차량의 재질 차이를 표현하였다.

### Particle System

환경용 눈 효과와 공격용 테이저 발사체 효과에 Particle System을 사용하였다.

테이저 발사체는 Prefab으로 만들어 코드에서 생성하도록 구성하였다.

### Visual Effect Graph

도둑의 피격 전기 효과를 VFX Graph로 구성하였다.

Spawn → Initialize → Update → Output 구조를 사용하여 전기 입자의 생성, 초기 상태, 움직임, 화면 출력 과정을 구성하였다.

### Code Integration

사용자의 공격 입력을 테이저 발사 이벤트와 연결하였다.

테이저가 목표 지점에 도달하면 적중 이벤트가 발생하고, 해당 이벤트를 통해 Beam 종료와 피격 VFX 생성이 실행된다.

또한 발사 중복과 적중 중복을 방지하여 하나의 공격 이벤트에서 불필요한 이펙트가 여러 번 생성되지 않도록 하였다.